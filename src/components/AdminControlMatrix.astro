---
interface Surface {
  surface: string;
  controller: string;
  controllerType: 'EOA' | 'Multisig' | 'Governance' | 'Timelock' | 'Contract' | 'Unknown';
  multisigConfig?: string;
  delayHours: number;
  worstCase: string;
}

interface Props {
  surfaces: Surface[];
  chain?: string;
}

import { explorerUrlFor } from '../utils/badges';

const { surfaces, chain = 'Ethereum' } = Astro.props;
const explorerUrl = explorerUrlFor(chain);

const typeColor: Record<string, string> = {
  EOA: 'bg-danger/10 text-danger border-danger/30',
  Multisig: 'bg-ok/10 text-ok border-ok/30',
  Governance: 'bg-info/10 text-info border-info/30',
  Timelock: 'bg-viol/10 text-viol border-viol/30',
  Contract: 'bg-ink/5 text-ink-2 border-line-2',
  Unknown: 'bg-ink/5 text-ink-3 border-line-2',
};

function formatDelay(hours: number) {
  if (hours <= 0) return { text: 'Instant', color: 'text-danger' };
  if (hours < 24)  return { text: `${hours}h`, color: 'text-warn' };
  if (hours < 48)  return { text: `${hours}h`, color: 'text-warn' };
  return { text: `${hours}h`, color: 'text-ok' };
}

function shortAddr(s: string) {
  const m = s.match(/0x[a-fA-F0-9]{40}/);
  if (!m) return s;
  return `${m[0].slice(0, 6)}…${m[0].slice(-4)}`;
}
---

<section id="trust" class="kyp-panel scroll-mt-28">
  <div class="flex items-baseline justify-between mb-4 flex-wrap gap-2">
    <h2 class="text-xl font-bold">Trust Surfaces</h2>
    <span class="text-xs text-ink-4">Who can move funds, and how fast</span>
  </div>

  {surfaces.length > 0 ? (
    <>
      <!-- Desktop: table -->
      <div class="hidden md:block overflow-x-auto">
        <table class="w-full text-sm">
          <thead>
            <tr class="border-b border-line-2 text-ink-3 text-left">
              <th class="pb-3 pr-4 font-semibold">Surface</th>
              <th class="pb-3 pr-4 font-semibold">Controller</th>
              <th class="pb-3 pr-4 font-semibold">Type</th>
              <th class="pb-3 pr-4 font-semibold whitespace-nowrap">Min Delay</th>
              <th class="pb-3 font-semibold">Worst Case</th>
            </tr>
          </thead>
          <tbody>
            {surfaces.map(s => {
              const delay = formatDelay(s.delayHours);
              const cleanAddr = s.controller.match(/0x[a-fA-F0-9]{40}/)?.[0];
              return (
                <tr class="border-b border-line-2 align-top hover:bg-ink/5 transition-colors">
                  <td class="py-3 pr-4 font-medium">{s.surface}</td>
                  <td class="py-3 pr-4 font-mono text-xs">
                    {cleanAddr ? (
                      <a href={`${explorerUrl}${cleanAddr}`} target="_blank" rel="noopener noreferrer" class="text-accent hover:text-accent-2">
                        {s.controller} ↗
                      </a>
                    ) : (
                      <span>{s.controller}</span>
                    )}
                  </td>
                  <td class="py-3 pr-4">
                    <span class={`inline-block px-2 py-0.5 rounded border text-xs font-semibold ${typeColor[s.controllerType] ?? typeColor.Unknown}`}>
                      {s.controllerType}{s.multisigConfig ? ` ${s.multisigConfig}` : ''}
                    </span>
                  </td>
                  <td class={`py-3 pr-4 font-semibold whitespace-nowrap ${delay.color}`}>{delay.text}</td>
                  <td class="py-3 text-ink-2">{s.worstCase}</td>
                </tr>
              );
            })}
          </tbody>
        </table>
      </div>

      <!-- Mobile: cards -->
      <div class="md:hidden space-y-3">
        {surfaces.map(s => {
          const delay = formatDelay(s.delayHours);
          const cleanAddr = s.controller.match(/0x[a-fA-F0-9]{40}/)?.[0];
          return (
            <div class="kyp-inset p-4">
              <div class="flex items-start justify-between gap-3 mb-2">
                <div class="font-medium text-ink leading-snug">{s.surface}</div>
                <span class={`shrink-0 inline-block px-2 py-0.5 rounded border text-[11px] font-semibold ${typeColor[s.controllerType] ?? typeColor.Unknown}`}>
                  {s.controllerType}{s.multisigConfig ? ` ${s.multisigConfig}` : ''}
                </span>
              </div>
              <div class="text-xs space-y-1.5">
                <div class="flex items-center gap-2">
                  <span class="text-ink-4 w-20 shrink-0">Controller</span>
                  {cleanAddr ? (
                    <a href={`${explorerUrl}${cleanAddr}`} target="_blank" rel="noopener noreferrer" class="font-mono text-accent hover:text-accent-2">{shortAddr(s.controller)} ↗</a>
                  ) : (
                    <span class="font-mono text-ink-2">{s.controller}</span>
                  )}
                </div>
                <div class="flex items-center gap-2">
                  <span class="text-ink-4 w-20 shrink-0">Min delay</span>
                  <span class={`font-semibold ${delay.color}`}>{delay.text}</span>
                </div>
                <div class="flex items-start gap-2">
                  <span class="text-ink-4 w-20 shrink-0">Worst case</span>
                  <span class="text-ink-2">{s.worstCase}</span>
                </div>
              </div>
            </div>
          );
        })}
      </div>
    </>
  ) : (
    <p class="text-ink-3">No trust-surface data available</p>
  )}
</section>
